<!DOCTYPE html>
<html lang="ru">
<head>
  <title>Bbq</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>


<div class="container">
  <header class="d-flex flex-wrap align-items-center justify-content-start py-3 mb-4 col-12 border-bottom">
    <div class="nav col-9 col-md-6 col-md-auto mb-2 mb-md-0">
      <li><%= link_to "Главная", root_path, class: 'navbar-brand nav-link px-2 link-secondary' %></li>
    </div>

    <% if user_signed_in? %>
      <div class="col-md-3 text-end">
        <%= link_to 'Профиль', current_user, class: 'btn btn-secondary' %>
        <%= link_to 'Выйти', destroy_user_session_path, :method => :delete, class: 'btn btn-outline-secondary me-2' %>
      </div>
    <% else %>
      <div class="col-md-3 text-end">
        <%= link_to 'Войти', new_user_session_path, class: 'btn btn-outline-secondary me-2' %></li>
        <%= link_to 'Регистрация', new_user_registration_path, class: 'btn btn-secondary' %></li>
      </div>
    <% end %>
  </header>

  <div class="row">
    <div class="col-lg-9">
      <% flash.each do |type, msg| %>
        <div class="alert <%= flash_alert_color_bg(type) %> alert-dismissible fade show" role="alert">
          <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>


    </div>


    <div class="col-lg-3 ">

    </div>

  </div>


  <footer>
    <p>&copy; Party Company <%= Time.now.year %></p>
  </footer>

</div>


</body>
</html>
